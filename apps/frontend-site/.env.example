# Только локальный Vite dev/preview; в Docker nginx обращается к api:8000.
BACKEND_API_URL=https://backend.localhost
# Для локального Caddy CA можно задать NODE_EXTRA_CA_CERTS при запуске npm.
# false допустим только для локального HTTPS с самоподписанным сертификатом.
BACKEND_TLS_VERIFY=true
